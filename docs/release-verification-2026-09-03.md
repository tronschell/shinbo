# Shinbo v0.4.1 release verification — September 3, 2026

Candidate commit: d51d07a9f744c78e7a972cb51041a3b4e4a0fbc7, prepared in `/private/tmp/shinbo-release-recovery-20260903`. PR https://github.com/tronschell/shinbo/pull/35 passed full CI and squash-merged into dev as 8dda06007a1e806387ab9bcf42ef6968b8b0a273. The local package uses the full ReleaseSafe harness build, with `SHINBO_FAST_BUILD` unset.

Promotion PR: https://github.com/tronschell/shinbo/pull/36, head 766f428b5a69746e8433e4f164271484bb85d942. Its complete tree matches both the tested candidate and the merged dev head. The promotion branch includes main's ancestry to satisfy the existing strict up-to-date rule while preserving dev's linear history. Its second full CI run passed in 13m30s. It merged into main as e023f351f41131b07bde759af6cdb0e3f28eea7f. The existing owner-only ruleset required the repository owner's administrator merge permission; GitHub explicitly confirmed that permission, all CI requirements were satisfied, and no protection rules were changed. Tree equality establishes source equivalence, not identical package bytes: the harness embeds its build commit, and the vendor dependency graph was not fully locked.

All six required local checks passed: the desktop check (957 tests, typecheck, lint, renderer build), Rust formatting, check, tests and Clippy, and the Zig harness tests. The desktop check passed after reinstalling its dependencies from the lockfile, exercising the fix for concurrent Electron installation.

The complete macOS package passed architecture, deployment-target, resource, license, native-helper, and strict ad-hoc code-signature checks. The copied search CLI runs through packaged Electron, and packaged ONNX, Transformers and llama modules import successfully after removal of the unused browser ONNX backend. The native search vendor directory is 267 MB.

The real packaged app was launched with an isolated profile and data root. It created and renamed a conversation, retained it after restart, attached an isolated folder, and ran `pwd` in its native terminal. To disambiguate multiple Shinbo copies for Computer Use, the UI smoke copy used `com.tronschell.shinbo.release-smoke-20260903` and was re-signed ad-hoc. The original package was not modified. Screenshot: `/private/tmp/shinbo-release-smoke-20260903/workspace.png`. All test app processes were stopped after verification.

Published release: https://github.com/tronschell/shinbo/releases/tag/v0.4.1. The release workflow https://github.com/tronschell/shinbo/actions/runs/33767469447 succeeded in 7m14s, including production signing, strict verification, notarization, stapling, Gatekeeper assessment, publication, and temporary credential cleanup. The release was published at 2026-09-03T14:39:47Z and is public, non-draft, and non-prerelease.

The public asset `Shinbo-v0.4.1-darwin-arm64.zip` is 204,537,325 bytes. Its SHA-256 is `e022807bc4fcfaed3ed2d33cb6268e976475ec02f635d65efcceca1d9301c487`. The asset and checksum were downloaded into `/private/tmp/shinbo-release-published-20260903`; `shasum -a 256 -c` passed. The extracted, unmodified app reports version 0.4.1 and bundle ID `com.tronschell.shinbo`. Strict deep codesign verification, stapler validation, and Gatekeeper assessment passed locally; Gatekeeper reports `source=Notarized Developer ID`.

The published app also satisfies the designated signing requirement extracted from the verified official v0.3.1 app. Both use Developer ID team `SE4VL5A535`. The official signed Electron runtime successfully imports ONNX, Transformers, and llama modules. This establishes signature continuity and module loading on this Mac, not an actual updater installation or compatibility with older macOS releases.

The destination GitHub tag `v0.4.1` was verified directly through the GitHub Git ref API and points to main commit `e023f351f41131b07bde759af6cdb0e3f28eea7f`. The local tag with that name points to an upstream FX commit, `36c94f94353a03b4d4b33540c028d16c85e50ed5`; it was preserved. Local unqualified tags are not valid release provenance in this checkout. The GitHub release API reports `immutable: false`; the repository's no-replacement policy was followed, but server-enforced immutability is not enabled for this release.

After publication, `https://update.electronjs.org/tronschell/shinbo/darwin-arm64/0.3.1` returned HTTP 200, selected `v0.4.1`, and pointed to the exact official ZIP URL. Before publication, this endpoint returned HTTP 204. The response and headers are retained alongside the downloaded asset.

A final launch of the unmodified official download with separate temporary data/profile paths exited with status 0; Computer Use could not retrieve its window while multiple same-ID Shinbo copies were registered. This attempt is not counted as a successful official-app UI smoke. The successful interaction smoke above used the explicitly identified ad-hoc copy. No user's running Shinbo process was stopped.

The process review identified and local `otool` checks confirmed an OS-support gap in the actual published native search payload: ONNX's binding and library require macOS 13.3, while four llama Metal binaries require macOS 14. The app advertises macOS 12, and the existing minimum-OS check covers top-level helpers but skips nested vendor directories. This does not prove that the entire app fails to launch on 12; it means these native search backends do not satisfy that advertised floor. Recursive native inventory and an explicit supported feature/OS decision are required before claiming full macOS 12 coverage.

Not exercised: global shortcuts, fresh macOS privacy grants, VoiceOver, multiple-display geometry, macOS 12 hardware, Intel hardware, Windows, and installation of an update between two official signed versions. Existing macOS privacy grants were observed in setup; they were not reset or changed. Folder-picker automation was unreliable with multiple copies, but the isolated folder attachment was verified in saved state and after restart.

The reusable release skill is installed at `/Users/tronschell/.agents/skills/desktop-release/SKILL.md`, with `/Users/tronschell/.claude/skills/desktop-release` linked to the same directory. Codex and Shinbo discover the shared `.agents/skills` root. The skill passed the skill-creator validator, local reference checks, and an independent offline rehearsal covering partial publication failure, pending required checks, untrusted artifacts, and conflicting upstream tags. The process recommendations are recorded in [the release process review](release-process-review-2026-09-03.md); they have not changed the active release workflow or repository settings.

Local skill discovery follows the [official Codex skill locations](https://learn.chatgpt.com/docs/build-skills#where-codex-loads-local-skills). The shared skill uses one canonical directory and a Claude compatibility link rather than separately maintained copies.
